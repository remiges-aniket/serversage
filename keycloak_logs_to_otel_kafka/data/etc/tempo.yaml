server:
  http_listen_port: 3200
  grpc_listen_port: 9095  # Ensure this port is free

storage:
  trace:
    backend: local
    local:
      path: /var/tempo/traces

compactor:
  compaction:
    block_retention: 1h  # Adjust retention as needed

distributor:
  receivers:
    otlp:
      protocols:
        grpc: {}
        http: {}